# Handwritten.cloud

```shell
docker build -t us-central1-docker.pkg.dev/hand-written-prod/docker/sveltekit:0.0.1 .
```

```shell
docker run --rm -p 3000:3000 handwritten-sveltekit:0.0.1
```

```shell
docker push us-central1-docker.pkg.dev/hand-written-prod/docker/sveltekit:0.0.1 
```
